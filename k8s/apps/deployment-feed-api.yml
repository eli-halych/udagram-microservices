apiVersion: apps/v1
kind: Deployment
metadata:
  name: feed-api
  labels:
    app: feed-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: feed-api
  template:
    metadata:
      labels:
        app: feed-api
    spec:
      containers:
        - name: udagram-feed-api
          image: 64523739/udagram-feed-api
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /feed
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 3
